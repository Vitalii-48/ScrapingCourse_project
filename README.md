🛒 ScrapingCourse Project
Цей проєкт — це повноцінний парсер для сайту scrapingcourse.com, побудований на базі Django. Він збирає дані про товари, обробляє їх (включаючи JSON-LD) та зберігає у базу даних для подальшого аналізу чи експорту.

🌟 Основні можливості
Глибокий парсинг: Збір назв, цін, описів, SKU та зображень.
Обробка метаданих: Вилучення та збереження структурованих даних у форматі JSON-LD.
Інтеграція з Django: Використання Django ORM для зручного зберігання та управління даними.
Експорт: Можливість генерації результатів у форматах CSV та JSON.
Модульність: Чіткий поділ на логіку парсингу, експорту та веб-інтерфейсу.

🛠 Технологічний стек
Backend: Python, Django (ORM & Admin panel)
Scraping: BeautifulSoup4, Requests
Database: PostgreSQL
Formats: JSON, CSV, JSON-LD

📂 Структура проєкту
├── ScrapingCourse_project/        # Django-проєкт (налаштування, urls, settings)
│   ├── __init__.py
│   ├── settings.py                # Конфігурація Django
│   └── urls.py                    # Маршрутизація
│
├── parser_app/                    # Основний Django-додаток
│   ├── migrations/                # Міграції бази даних
│   ├── __init__.py
│   ├── admin.py                   # Реєстрація моделей в адмінці
│   └── models.py                  # Модель Product та інші
│
├── modules/                       # Допоміжні скрипти для парсингу
│   ├── 1_get_listings.py          # Парсинг списку товарів
│   ├── 2_get_info.py              # Парсинг деталей товарів
│   ├── load_django.py             # Ініціалізація Django середовища
│   └── status.py                  # Перевірка статусу парсингу
│
├── json/                          # Експортовані JSON-файли товарів
│   ├── *.json                     # дані про товар у форматі JSON (ігнорується у Git)
│   ├── export_json_ld.py          # Експорт JSON-LD у файли
│   └── readme.md                  # Документація для JSON-експорту
│
├── results/                       # Результати парсингу
│   ├── products.csv               # Експорт у CSV категорії (списку) товарів (ігнорується у Git)
│   ├── products_csv.py            # Скрипт для генерації CSV
│   ├── products_dump.json         # Експорт категорії (списку) товарів у JSON (ігнорується у Git)
│   └── products_dump.py           # Скрипт для генерації JSON
│
├── files/                         # Додаткові збережені сторінки чи документи
│
├── .venv/                         # Віртуальне середовище (ігнорується у Git)
├── .env                           # Конфігурація середовища (ігнорується у Git)
├── .gitignore                     # Виключені файли/папки
├── manage.py                      # Django-утиліта
└── requirements.txt               # Залежності проєкту

🚀 Швидкий старт
1. Підготовка середовища
bash
# Клонуйте репозиторій
git clone <ваш-url-репозиторію>
cd ScrapingCourse_project

# Створіть та активуйте віртуальне середовище
python -m venv .venv
source .venv/bin/activate  # Для Linux/macOS
.venv\Scripts\activate     # Для Windows

# Встановіть залежності
pip install -r requirements.txt

2. Налаштування бази даних
bash
python manage.py migrate

3. Запуск парсингу
Для того, щоб зібрати дані з сайту та зберегти їх у БД, запустіть:
bash
python modules/1_get_listings.py

4. Перегляд результатів
Ви можете переглянути зібрані дані в консолі:
bash
python modules/2_get_info.py
Або через Django Admin (спочатку створіть суперкористувача python manage.py createsuperuser та запустіть python manage.py runserver).

📂 Експорт результатів
Проєкт підтримує кілька варіантів вивантаження даних:
У CSV: python results/products_csv.py (файл з'явиться в results/products.csv)
У загальний JSON: python results/products_dump.py
Окремі JSON-LD файли: python json/export_json_ld.py

📊 Модель даних (Product)
Кожен товар містить наступні поля:
name — назва товару.
url — пряме посилання на сторінку.
price — ціна (конвертована в число).
description — повний опис товару.
sku — унікальний артикул.
json_ld — сирі структуровані дані.

⚠️ Примітка щодо етики (Disclaimer)
Цей проєкт створений виключно з навчальною метою на базі спеціального ресурсу для практики скрапінгу. Будь ласка, використовуйте time.sleep(), щоб не перевантажувати сервери.