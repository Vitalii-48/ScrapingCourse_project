# 🛍️ Product JSON-LD Exporter

## Опис
Ця папка в проєкті призначена для експорту даних з моделі **Product** у форматі **JSON-LD**.  
Скрипт `export_json_ld.py` автоматично створює окремі `.json` файли для кожного товару в папці `json/`.

## Структура папки json

- **json/** — каталог із JSON‑файлами товарів  
- **export_json_ld.py** — скрипт експорту з Django‑моделі  
- **README.md** — документація  

## Використання
1. Переконайтеся, що у вас налаштований Django‑проєкт із моделлю `Product`, яка має поля:
   - `name` — назва товару
   - `json_ld` — JSON‑LD дані товару

2. Запустіть скрипт:
   ```bash
   python json/export_json_ld.py